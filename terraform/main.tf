resource "juju_application" "dex_auth" {
  charm {
    name     = "dex-auth"
    channel  = var.channel
    revision = var.revision
  }
  config    = var.config
  model     = var.model_name
  name      = var.app_name
  resources = var.resources
  trust     = true
  units     = 1
}
